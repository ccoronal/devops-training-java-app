// FUNCIONES
def runInDocker(String image, Closure pipeline) {
    docker.image(image).inside { pipeline() }
}

// PIPELINE
pipeline {

    agent any

    stages {

        stage('Info') {
            steps {
                echo "Info"
                echo "Job: ${env.JOB_NAME}"
                echo "Build: ${env.BUILD_NUMBER}"
                echo "Branch: ${env.BRANCH_NAME}"
            }
        }

        stage('CI - Build tools image') {
            steps {
                script {
                    docker.build(env.CI_IMAGE_NAME, "-f devops/ci.Dockerfile .")
                }
            }
        }

        stage('CI - Lint') {
            steps {
                script {
                    devopsLib.runInDocker(env.CI_IMAGE_NAME) {
                        sh 'make lint'
                    }
                }
            }
        }

        stage('CI - Test') {
            steps {
                script {
                    devopsLib.runInDocker(env.CI_IMAGE_NAME) {
                        echo "Unit test"
                        sh 'make test'
                    }
                }
            }
        }
        // final del bloque de stages
    }
}